[build]
  command = "cd front-end && rm -rf node_modules package-lock.json && npm install && npm run build"
  publish = "front-end/dist"

[context.production.environment]
  VITE_API_URL = "https://sian-d-acqui-production.up.railway.app/api"
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
